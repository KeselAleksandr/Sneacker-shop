import React, { useContext } from 'react'
import AppContext from '../context'

const info = () => {
  const {} = useContext(AppContext)
  return (
    <div>
      
    </div>
  )
}

export default info
